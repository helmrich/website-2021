/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
  // Doesn't work for SSG right now
  // i18n: {
  //   locales: ['en', 'de'],
  //   defaultLocale: 'en',
  // },
};
